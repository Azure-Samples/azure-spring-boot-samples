spring:
  cloud:
    azure:
      profile:
        subscription-id: ${AZURE_SUBSCRIPTION_ID}
      redis:
        name: ${AZURE_CACHE_REDIS_DNS_NAME}
        resource:
          resource-group: ${AZURE_RESOURCE_GROUP}
