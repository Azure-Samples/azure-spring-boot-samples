spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Azure-Samples/spring-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
