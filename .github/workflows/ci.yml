# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: CI

on: [push, pull_request]

jobs:
  java-ci-with-maven-adopt:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout azure-spring-boot-samples from feature/spring-boot-3 branch
      uses: snow-actions/sparse-checkout@v1.1.0
    - name: Checkout azure-sdk-for-java from feature/spring-boot-3 branch
      with:
        patterns: |
          sdk/spring sdk/jdbc sdk/spring-experimental eng sdk/boms sdk/parents
        repository: Azure/azure-sdk-for-java
        ref: feature/spring-boot-3
        path: azure-sdk-for-java
    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: maven
    - name: Display maven environment
      run: mvn -v
    - name: Build Spring Cloud Azure libraries with Maven
      run: |
          cd azure-sdk-for-java
          mvn clean install -Dmaven.javadoc.skip=true -DskipTests \
            -Dcheckstyle.skip=true \
            -ntp \
            -Dspotbugs.skip=true \
            -Drevapi.skip=true -Djacoco.skip=true \
            -Dparallel-test-playback \
            -Pdev \
            -f sdk/spring/pom.xml
    - name: Verify samples with Maven
      run: mvn -f pom.xml --batch-mode --update-snapshots verify

